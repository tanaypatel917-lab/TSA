import { glossary } from "@/content/glossary";

export default function GlossaryPage() { return <div className="shell py-12"><p className="eyebrow">Words to know</p><h1 className="mt-2 text-4xl font-black">AI glossary</h1><p className="mt-4 max-w-2xl text-lg text-slate-600">Keep this page nearby while you learn. Understanding the vocabulary makes better questions possible.</p><dl className="mt-8 grid gap-4 sm:grid-cols-2">{glossary.map((item) => <div className="card" key={item.term}><dt className="text-lg font-bold text-accent">{item.term}</dt><dd className="mt-2 text-slate-600">{item.definition}</dd></div>)}</dl></div>; }
