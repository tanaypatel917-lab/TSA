"use client";

import Link from "next/link";
import { modules } from "@/content";
import { BADGES, evaluateBadges } from "@/engine/badges";
import { levelFor, nextLevel, progressToNext } from "@/engine/levels";
import { exportProgress } from "@/engine/storage";
import { useProgress } from "@/state/ProgressProvider";
import { ModuleList } from "@/components/ModuleList";
import { ProgressBar } from "@/components/ProgressBar";
import { HeroDial } from "@/components/HeroDial";
import { MagneticButton } from "@/components/motion/MagneticButton";
import { Reveal } from "@/components/motion/Reveal";

function modulePercent(moduleId: string, lessons: number, state: ReturnType<typeof useProgress>["state"]) {
  const done = state.completedLessons.filter((key) => key.startsWith(`${moduleId}/`)).length;
  return Math.round(((done + (state.completedActivities.includes(moduleId) ? 1 : 0) + (state.quizBest[moduleId] ?? 0) / 100) / (lessons + 2)) * 100);
}

function Hero() {
  return <><section className="relative flex min-h-[92svh] items-center overflow-hidden border-b border-line pt-20"><div className="shell grid w-full items-center gap-8 py-20 lg:grid-cols-12"><div className="relative z-10 lg:col-span-8"><p className="eyebrow"><span className="mr-3 text-accent">00</span> AI literacy for the next generation</p><h1 className="mt-8 max-w-5xl break-words font-display text-[clamp(3rem,9vw,8.5rem)] leading-[.92] tracking-[-.03em]">Find your way through <em className="block text-accent sm:inline">artificial</em> intelligence.</h1><div className="mt-10 grid gap-6 sm:grid-cols-2"><p className="max-w-prose text-lg leading-relaxed text-ink/70">Understand how AI works, practice asking better questions, and decide how you want technology to shape your world.</p><MagneticButton href="/modules">Begin the journey</MagneticButton></div></div><div className="pointer-events-none absolute -right-24 top-32 hidden w-[55vw] max-w-[620px] opacity-70 sm:block lg:right-0 lg:top-1/2 lg:-translate-y-1/2"><HeroDial /></div></div><div className="absolute bottom-0 left-0 right-0 overflow-hidden border-t border-line py-3"><div className="marquee-track flex w-max gap-8 whitespace-nowrap font-mono text-[10px] uppercase tracking-[.24em] text-ink/65">Foundations · Tools · Ethics · Real world · Capstone · Foundations · Tools · Ethics · Real world · Capstone · Foundations · Tools · Ethics · Real world · Capstone ·</div></div></section><section className="bg-dark py-24 text-paper sm:py-36"><div className="shell grid gap-14 md:grid-cols-3"><Reveal><p className="font-mono text-7xl text-paper/20">01</p><h2 className="mt-7 font-display text-5xl italic">Understand</h2><p className="mt-5 max-w-xs text-paper/60">Build a clear mental model before you build with AI.</p></Reveal><Reveal delay={.08}><p className="font-mono text-7xl text-paper/20">02</p><h2 className="mt-7 font-display text-5xl italic">Practice</h2><p className="mt-5 max-w-xs text-paper/60">Try prompts, scenarios, and projects where your choices matter.</p></Reveal><Reveal delay={.16}><p className="font-mono text-7xl text-paper/20">03</p><h2 className="mt-7 font-display text-5xl italic">Decide</h2><p className="mt-5 max-w-xs text-paper/60">Use curiosity, evidence, and care to choose your next step.</p></Reveal></div></section></>;
}

export default function Dashboard() {
  const { state, reset, importJson } = useProgress();
  const level = levelFor(state.xp); const next = nextLevel(state.xp);
  function upload(event: React.ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (!file) return; const reader = new FileReader(); reader.onload = () => importJson(String(reader.result)); reader.readAsText(file); }
  function download() { const link = document.createElement("a"); link.href = URL.createObjectURL(new Blob([exportProgress(state)], { type: "application/json" })); link.download = "ai-compass-progress.json"; link.click(); }
  if (!state.startedAt) return <><Hero /><section className="shell py-24 sm:py-36"><div className="mb-12 grid gap-6 lg:grid-cols-12"><div className="lg:col-span-7"><p className="eyebrow"><span className="mr-3 text-accent">01</span> The learning path</p><h2 className="mt-6 font-display text-[clamp(2rem,4.5vw,3.75rem)] leading-none">Five directions.<br /><em>One compass.</em></h2></div><p className="self-end text-lg leading-relaxed text-ink/65 lg:col-start-8 lg:col-span-4">Short lessons and hands-on exercises for the questions that matter when everyone is learning what AI can do.</p></div><ModuleList modules={modules} /></section></>;
  const badges = evaluateBadges(state, modules);
  const progress = Object.fromEntries(modules.map((module) => [module.id, modulePercent(module.id, module.lessons.length, state)]));
  const overall = Math.round(Object.values(progress).reduce((a, b) => a + b, 0) / modules.length);
  return <div className="shell py-32"><div className="grid gap-10 lg:grid-cols-12"><div className="lg:col-span-8"><p className="eyebrow"><span className="mr-3 text-accent">00</span> Your dashboard</p><h1 className="mt-6 font-display text-[clamp(3.5rem,8vw,7rem)] leading-[.88]">Keep finding<br /><em>your way.</em></h1></div><p className="self-end text-lg leading-relaxed text-ink/65 lg:col-start-9 lg:col-span-4">Small steps become a point of view. Return when you are ready for the next question.</p></div><section className="mt-20 grid gap-px bg-line sm:grid-cols-3"><div className="bg-paper p-7"><p className="eyebrow">Current level</p><p className="mt-6 font-display text-5xl italic">{level.name}</p><p className="mt-3 font-mono text-4xl">{state.xp} <span className="text-sm text-ink/50">XP</span></p></div><div className="bg-paper p-7"><p className="eyebrow">Overall progress</p><p className="mt-6 font-mono text-6xl">{overall}<span className="text-2xl">%</span></p><ProgressBar value={progressToNext(state.xp) * 100} label={next ? `${next.minXp - state.xp} XP to ${next.name}` : "Maximum level"} /></div><div className="bg-paper p-7"><p className="eyebrow">Streak</p><p className="mt-6 font-mono text-6xl">{state.streak.count}<span className="text-2xl"> days</span></p><p className="mt-3 text-ink/60">Keep the signal alive.</p></div></section><div className="mt-16 flex flex-wrap gap-5 border-b border-line pb-6"><button className="btn-ghost" onClick={download}>Export progress ↗</button><label className="btn-ghost cursor-pointer">Import progress ↗<input type="file" accept="application/json" className="sr-only" onChange={upload} /></label><button className="btn-ghost" onClick={() => { if (window.confirm("Reset all local progress?")) reset(); }}>Reset ↗</button></div><section className="mt-24"><div className="mb-10 flex items-end justify-between"><div><p className="eyebrow"><span className="mr-3 text-accent">01</span> Continue</p><h2 className="mt-5 font-display text-5xl italic">Your modules</h2></div><Link href="/modules" className="btn-ghost">All modules ↗</Link></div><ModuleList modules={modules} progress={progress} compact /></section><section className="mt-24"><p className="eyebrow"><span className="mr-3 text-accent">02</span> Collected signals</p><h2 className="mt-5 font-display text-5xl italic">Badge shelf</h2><div className="mt-8 flex flex-wrap gap-3">{BADGES.map((badge) => <span key={badge.id} className={`rounded-full border px-4 py-2 font-mono text-[10px] uppercase tracking-wider ${badges.includes(badge.id) ? "border-lime bg-lime" : "border-line opacity-40"}`}>{badge.icon} {badge.name}</span>)}</div></section></div>;
}
